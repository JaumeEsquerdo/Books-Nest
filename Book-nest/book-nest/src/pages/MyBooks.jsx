

const MiPage = () => {
    return ( 
        <div>
            <h1>Mis libros</h1>
            <p>Esta es mi página</p>
            {/* <ul>
                {myBooks.map((book) => (
                    <li key={book.id}>{book.title}</li>
                ))}
            </ul> */}
        </div>

     );
}
 
export default MiPage;